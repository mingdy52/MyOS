node_modules
# Keep environment variables out of version control
.env

# 동적 발견한 노션 스키마 캐시 (언제든 refresh로 다시 만들 수 있음)
.cache

/src/generated/prisma

# 포트폴리오 사이트는 Actions가 빌드해 배포한다 → 생성물은 커밋하지 않는다
/docs

# macOS
.DS_Store
